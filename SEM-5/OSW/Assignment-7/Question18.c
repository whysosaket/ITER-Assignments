#include <stdio.h>

int main()
{
    int b = 65;
    // void p = b;
    // printf("%d", p);
    return 0;
}

/*
Incompatible types in assignment (expression has type "int", void cannot be assigned to it)
*/