#include <stdio.h>
#include <string.h>

int main() {
    char pmsg[] = "I am in 5th Sem CSE";   // -> Type String, or array of characters
    char *amsg = "I am in 5th Sem CSE";    // -> Pointer to String, or pointer to array of characters
    return 0;
}
